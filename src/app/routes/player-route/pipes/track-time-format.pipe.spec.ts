import { TrackTimeFormatPipe } from './track-time-format.pipe';

describe('TrackTimeFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new TrackTimeFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
